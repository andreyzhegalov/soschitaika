server:
  port: 8083
  error:
    include-stacktrace: always


management:
  endpoints:
    web:
      exposure:
        include: metrics,health
  endpoint:
    health:
      show-details: always

logging:
  level:
    ROOT: INFO
    org.springframework: INFO
